<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Minimalist Warm Palette -->
    
    <!-- Backgrounds & Surfaces (Crema Claro) -->
    <Color x:Key="SurfaceColor">#FFF6E5</Color>      <!-- Color base crema clarito -->
    <Color x:Key="SurfaceAltColor">#FFFFFF</Color>   <!-- Blanco puro para tarjetas/paneles sobre el crema -->
    
    <!-- Brand / Primary Actions (Naranja) -->
    <Color x:Key="PrimaryColor">#F47920</Color>      <!-- Naranja principal para botones fuertes y links -->
    <Color x:Key="PrimaryHoverColor">#E06B1B</Color> <!-- Naranja más oscuro para hover -->

    <!-- Secondary / Accents (Mostaza) -->
    <Color x:Key="SecondaryColor">#FBB03B</Color>    <!-- Mostaza para iconos de menú, alertas, bordes sutiles -->
    
    <!-- Danger / Destructive Actions (Coral/Rojo) -->
    <Color x:Key="DangerColor">#EF4136</Color>       <!-- Rojo coral para botones eliminar / errores -->
    <Color x:Key="DangerHoverColor">#D63428</Color>  <!-- Rojo coral más oscuro para hover -->

    <!-- Text Colors -->
    <Color x:Key="TextPrimaryColor">#2D3748</Color>  <!-- Gris muy oscuro en lugar de negro puro para suavizar -->
    <Color x:Key="TextSecondaryColor">#718096</Color><!-- Gris medio para placeholders o descripciones -->
    <Color x:Key="TextOnPrimaryColor">#FFFFFF</Color><!-- Blanco puro sobre botones Naranja/Rojo -->

    <!-- Borders / Dividers -->
    <Color x:Key="BorderColor">#E2E8F0</Color>       <!-- Gris claro elegante para divisiones o inputs -->

    <!-- Brushes -->
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceAltBrush" Color="{StaticResource SurfaceAltColor}"/>
    
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{StaticResource PrimaryHoverColor}"/>
    
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
    
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}"/>
    <SolidColorBrush x:Key="DangerHoverBrush" Color="{StaticResource DangerHoverColor}"/>

    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextOnPrimaryBrush" Color="{StaticResource TextOnPrimaryColor}"/>
    
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

</ResourceDictionary>
